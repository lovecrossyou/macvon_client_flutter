import 'package:flutter/material.dart';

class VirtualCardScene extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('VirtualCardScene'),),
    );
  }
}